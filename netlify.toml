[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: every route falls through to the app shell.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
