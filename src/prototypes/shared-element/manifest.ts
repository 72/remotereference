import type { PrototypeManifest } from "../../registry";

export const manifest: PrototypeManifest = {
  slug: "shared-element",
  title: "Shared element",
  blurb:
    "A card morphs into a full detail screen while the header persists and changes state in place. Drag the hero down to dismiss.",
  objectives: ["2.1 continuity", "2.2 morph", "2.4 interruptible", "2.5 velocity"],
  accent: ["#0ea5e9", "#4338ca"],
};
