import type { PrototypeManifest } from "../../registry";

export const manifest: PrototypeManifest = {
  slug: "bottom-sheet",
  title: "Bottom sheet",
  blurb:
    "Detents with velocity settling and rubber-banding. Drag the list at the top of its scroll to hand the gesture back to the sheet.",
  objectives: ["2.3 direct", "2.4 interruptible", "2.5 velocity"],
  accent: ["#14b8a6", "#0f766e"],
};
